import math

"""
Different standard parameters for scenes
"""

walls_texture = {
        'type': 'color',
        'color': (150, 150, 0)
}

basic_scene_default = {
    'scene_type': 'room',
    'shape': (400, 400),
    'walls_depth': 10,
    'walls_texture': walls_texture
    }

